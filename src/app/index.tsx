import { Text, View } from "react-native";

export default function Index() {
  return (
    <View className="h-full bg-blue-500">
      <Text className="text-red-500 text-lg font-">NATIVE</Text>
    </View>
  );
}
